<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The first vector drawable (bottom layer) -->

    <item android:drawable="@drawable/confirm"
        />

    <!-- The second vector drawable (top layer) with an offset -->
    <item
        android:drawable="@drawable/baseline_key_24"
        android:width="12dp"
        android:height="12dp"
        android:top="8dp"
        android:right="8dp"
        android:left="6dp"
        android:bottom="8dp" />
</layer-list>
